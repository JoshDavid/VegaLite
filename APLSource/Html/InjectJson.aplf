 InjectJson←{
⍝ ⍺ ←→ HtmlRenderer object
⍝ ⍵ ←→ JSON spec
     h←⍺.HTML
     h←h Inject'<head>'(InnerHead'')
     h←h Inject'<body>'(InnerBody ⍵)
     (script btn)←SavePngButton''
     h←h Inject'<head>'script
     h←h Inject'<body>'btn
     ⍺.HTML←h
     ⍺
 }
