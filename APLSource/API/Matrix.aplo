 Matrix←{
 ⍝ Operator -- all combinations of ⍵
 ⍝ Limited to 2D comparisons currently
 ⍝ AND type of quantitative for now
 ⍝ ⍺  ←→ Optional ParamNS
 ⍝ ⍺⍺ ←→ Plot fn
 ⍝ ⍵  ←→ Data
     ⍺←##.Utils.Alpha''
     o←⍺.SpecOverride
     r←⍎'repeat'o.⎕NS''
     h←,1↑⍵
     r.row←h
     r.column←⌽h
     s←⍎'spec' o.⎕NS ''
     e←⍎'encoding's.⎕NS''
     _←'xy'{
         t←⍎⍺ e.⎕NS''
         t.type←'quantitative'
         f←⍎'field't.⎕NS''
         f.repeat←⍵ ⋄ 0
     }¨'column' 'row'
     ⍺ ⍺⍺ ⍵
 }
