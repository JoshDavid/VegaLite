 CorrelationHeatMap←{
 ⍝ ⍵ ←→ Data for HeatMap function
 ⍝ Special case of HeatMap used for viewing correlations
     a←##.Utils.Alpha''
     o←a.SpecOverride
     s←⍎'encoding.color.scale' o.⎕NS ''
     s.scheme←'redblue'
     s.domain←¯1 1
     a ##.API.HeatMap ⍵
 }
