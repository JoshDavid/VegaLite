 SavePng←{
⍝ ⍵ ←→ base64 encoded PNG data URL
     b64←'data:image/png;base64,'
     b64≢⍵↑⍨≢b64:11 ⎕SIGNAL'not a base 64 data URL'
     img←⍵↓⍨≢b64
     tn←'/tmp/graph.png'⎕NCREATE⍠'IfExists' 'Replace'⊢0
     data←⎕UCS base64 img
     data ⎕NAPPEND tn
 }
