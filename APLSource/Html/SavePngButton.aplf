 SavePngButton←{
⍝ ⍵ ←→ HTML text
     btn←'<button onclick="savePng()">Savg PNG</button>',NL
     script←'<script>',NL
     script,←'socket = new WebSocket("ws://dyalog_root/")',NL
     script,←'const Send = (data) => { socket.send(JSON.stringify(data)) }',NL
     script,←'const savePng = (_) => {Send("Hello"); Send (document.getElementsByClassName("marks")[0].toDataURL("image/png"))}',NL
     script,←'</script>',NL
     script btn
 }
